---
# Sets up the gateway api for  vaultwarden
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: paperless-ngx-gateway
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "21"
spec:
  project: default
  sources:
    - repoURL: https://github.com/it-dfl/homelab.git
      targetRevision: HEAD
      path: argocd/prod/gateway-api-static-vlan-gateway
      helm:
        valueFiles: # all default overrides are in the private repo
          - $private_repo/argocd/prod/paperless-ngx/gateway-values.yaml
    - repoURL: https://github.com/it-dfl/homelab-private.git
      targetRevision: HEAD
      ref: private_repo
  destination:
    server: https://kubernetes.default.svc
    namespace: paperless-ngx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
